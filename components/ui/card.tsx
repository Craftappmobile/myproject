import React from "react"; import { View } from "react-native"; import { cn } from "@/lib/utils";  type CardProps = {   children: React.ReactNode;   className?: string; };  export function Card({ children, className }: CardProps) {   return (     <View       className={cn(         "bg-card rounded-lg border border-border shadow-sm",         className       )}     >       {children}     </View>   ); }
